#include "MultiArray3D.h"

MultiArray3D::MultiArray3D(int rx, int ry, int rz): MultiArray(){
    std::vector<int> v(3);
    v[0] = rx;
    v[1] = ry;
    v[2] = rz;

    resize(v);
}

MultiArray3D::~MultiArray3D(){
}

