#include "MultiArray.h"

MultiArray::MultiArray(){
}

MultiArray::MultiArray(const std::vector<int> &orig_range_){
    resize(orig_range_);
}

MultiArray::MultiArray(const MultiArray *orig){
    copy(orig);
}

MultiArray::MultiArray(const MultiArray &orig){
    copy(&orig);
}

MultiArray::~MultiArray(){
}

